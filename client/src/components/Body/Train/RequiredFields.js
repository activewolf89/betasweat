  import React,{Component} from "react";

  class RequiredFields extends Component{
    render(){
      // var inputs = Object.keys(this.props.requiredFields).map((key)=>{
      //   return (
      //     <input name={key}></input>
      //   )
      // })
      console.log(this.props.requiredFields)
      return(
        // {inputs}
        <h1>Yo</h1>
      )
  }
}
  export default RequiredFields;
